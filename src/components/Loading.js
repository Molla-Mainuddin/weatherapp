import React from 'react';

const Loading = ()=>{

    return(
        <>
            <div className="loader">
                <p>Loading...</p>
            </div>
        </>
    );
};

export default Loading;